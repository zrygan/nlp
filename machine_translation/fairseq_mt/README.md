
# COMMANDS

```zsh
uv run --active fairseq-preprocess \
--source-lang tgl \
--target-lang ceb \
--trainpref data/train.ceb-tgl \
--destdir data-bin/ceb-tgl \
--validpref data/valid.ceb-tgl \
--testpref data/test.ceb-tgl
```
